# 🚀 Rapid Stack

Una plantilla full‑stack pensada para entender y arrancar rápido: frontend en React y backend en Spring Boot (si lo agregas). Clara, práctica y lista para prototipos.

¿Qué es?
- Frontend React listo para desarrollo y conexión a una API REST.
- Carpetas organizadas para separar UI, utilidades y configuración.

Arranca en 3 pasos (rápido y sin vueltas)
1) Clona
```bash
git clone https://github.com/EduMoralesCar/Rapid_Stack.git
cd Rapid_Stack
```
2) Frontend (React)
```bash
cd React
npm install
npm start
# Abre http://localhost:3000
```

Tecnologías
- ⚛️ React — v18+
- 🟩 Node.js — v18.x (npm v9+)
- ☕ Java — 11+ (para Spring Boot)
- 🧰 Spring Boot — 2.7 / 3.x (según JDK)
- 📦 npm / yarn — npm v9+ o yarn 1/berry
- 🐳 Docker — opcional para deploy

